import SignupClient from '@/components/signup-client';

/**
 * Signup page component
 * Renders the signup form with email and OAuth options
 */
export default function SignupPage() {
  return (
    <SignupClient />
  );
}
